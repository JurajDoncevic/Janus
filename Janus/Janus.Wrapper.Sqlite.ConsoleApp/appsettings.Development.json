{
  "ComponentOptions": {
    "NodeId": "DebugWrapperNode1",
    "ListenPort": 10001,
    "TimeoutMs": 5000,
    "CommunicationFormat": "AVRO",
    "NetworkAdapterType": "TCP",
    "StartupRemotePoints": [],
    "SourceConnectionString": "Data Source=./chinook.db",
    "AllowsCommands": true
  },

  "NLog": {
    "targets": {
      "file": {
        "type": "File",
        "fileName": "./log-${shortdate}.log",
        "layout": "${longdate}|${logger}|${message}"
      }
    },
    "rules": [
      {
        "logger": "*",
        "minLevel": "Info",
        "writeTo": "file"
      }
    ]
  }
}
